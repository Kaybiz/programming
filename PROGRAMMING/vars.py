# Variables

my_name = 'Kayode'
my_age = 21

"""
Variables naming rules:
Must start with a letter  (a-z, A-Z)or an underscore (-)
Can contain letters, numbers and underscores
Are case sensitive (age, Age, AGE are different variables)
Cannot be python keywords like ('if' 'for', 'while', etc.)

"""

instance_name = "web-server" #String: paython knows its a string based on the value we have given it
instance_count = 4 #Integer
is_running = True #Boolean variable

