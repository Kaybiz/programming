// --- If / Else Statement

let cloudProvider = "AWS";

if (cloudProvider === "AWS") {
    console.log(" We are using Amazon Web Services");
}
else {
    console.log(" We are not using Amazon Web Services");
}


// --- Iac tool ---
let IaC = "Terraform";

if (IaC === "Terraform") {
    console.log(" We are using Terraform");
}
else {
    console.log(" We are not using Terraform");
}